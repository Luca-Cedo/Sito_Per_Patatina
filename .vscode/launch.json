{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "pwa-chrome",
            "request": "launch",
            "name": "Apri il sito",
            "file": "${workspaceFolder}/index.html"
        }
    ]
}